employee tracker. I had to remake the github repository was not letting me push to origin for some reason.

to run clone it down. edit seed to need, run npm i in the terminal, than node index in the server